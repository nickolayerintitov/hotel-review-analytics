{
  "buildCommand": "cd hotel-review-app && npm run build",
  "outputDirectory": "hotel-review-app/dist",
  "installCommand": "cd hotel-review-app && npm install",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
